#include <stdio.h>

/**
 * main -rpints a given phrase
 * return : 0 if exited properly , non-zero otherwise
 */

int main(void)
{
	puts("\"Programming is like building and multilingual puzzle");
	return (0);
}
